//import { Fraction } from "./../../../src/"
//TODO: fraction multiplication tests
describe("Fraction multiplication", () => {
  test("Fraction multiplication - 1", () => {
    expect(1).toBe(1)
  })
})
